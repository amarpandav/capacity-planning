package com.ubs.tools.cpt.core.user;

/**
 * There are multiple potential sources of user data.
 * CS has its own systems, so does UBS.
 *
 * For now <b>only AURA is supported</b>.
 */
public enum UserSystem {
    AURA
}
